{
  "collectionName": "components_pricing_price_tables",
  "info": {
    "displayName": "Pricing Table",
    "description": "Step 1 — add Columns (label + optional icon). Step 2 — add Rows (one plan per row). Each row needs one Cell per column, in the same order."
  },
  "attributes": {
    "columns": {
      "type": "component",
      "component": "pricing.table-column",
      "repeatable": true
    },
    "rows": {
      "type": "component",
      "component": "pricing.table-row",
      "repeatable": true
    },
    "c1Label": {
      "type": "string"
    },
    "c2Label": {
      "type": "string"
    },
    "c3Label": {
      "type": "string"
    },
    "c4Label": {
      "type": "string"
    },
    "priceLabel": {
      "type": "string"
    },
    "caption": {
      "type": "text"
    },
    "noteText": {
      "type": "text"
    },
    "noteType": {
      "type": "enumeration",
      "default": "info",
      "enum": [
        "info",
        "warning"
      ]
    }
  }
}
